// 44. Create a function that will convert a string containing a binary number into a
// number

function binaryToInt(binary) {
    return parseInt(binary, 3)
}

console.log(binaryToInt("101110101"));
