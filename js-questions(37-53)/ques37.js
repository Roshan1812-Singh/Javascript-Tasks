// 37. Create a function that will convert a string in an array containing the ASCII codes of
// each character

let str = "Hello $sir"

let asciiValues = Array.from(str, (char) => char.charCodeAt(0));
console.log(str.split());

console.log(asciiValues);
